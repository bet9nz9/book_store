/**
 * JSONP Interceptor.
 */

import jsonpClient from 'vue-resource/src/http/client/jsonp';

export default function (request) {

    if (request.method == 'JSONP') {
        request.client = jsonpClient;
    }

}
