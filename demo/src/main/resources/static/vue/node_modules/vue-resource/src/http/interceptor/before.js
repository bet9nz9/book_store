/**
 * Before Interceptor.
 */

import {isFunction} from 'vue-resource/src/util';

export default function (request) {

    if (isFunction(request.before)) {
        request.before.call(this, request);
    }

}
